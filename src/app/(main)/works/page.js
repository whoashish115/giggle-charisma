import React from "react";
import { Works as WorksComponent } from "@/components";

export const metadata = {
 title: 'Works',
 description: 'Works page of the website',
 keywords: 'Works, website',} 
 
export default function Works() {
  return (
    <WorksComponent />
  );
}